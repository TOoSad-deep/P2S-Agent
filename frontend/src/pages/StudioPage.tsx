// StudioPage.tsx — build/analysis view. PngShaderView reads everything from
// PngShaderContext, so this page needs no props.
import PngShaderView from "../components/PngShaderView";

export default function StudioPage() {
  return <PngShaderView />;
}
